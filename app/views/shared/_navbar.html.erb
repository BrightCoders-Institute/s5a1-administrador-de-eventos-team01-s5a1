<nav class="bg-black h-28 w-screen flex flex-row ">
  <div class="mx-4 py-5 flex items-center">
    <%= image_tag("logo.png", class:"my-0 h-4/5") %>
  </div>

  <div class="mx-4 flex items-center">
    <%= print_home_link %>
  </div>

  <% if user_signed_in? %>
    <div class="mx-4 flex items-center">
      <%= print_get_started_link %>
    </div>
  <% end %>

  <div class="mx-4 flex items-center">
    <%= user_signed_in? ? print_sign_out_button : print_sign_in_link %>
  </div>
</nav>
