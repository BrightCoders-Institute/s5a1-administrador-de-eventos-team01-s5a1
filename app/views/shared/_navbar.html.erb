<nav class="sticky top-0 bg-slate-800 h-fit w-full flex flex-row">
  <div class="w-full py-5 px-4 md:py-7 md:pl-6 md:pr-9 flex flex-col md:flex-row items-center">
    <div>
      <%= image_tag "logo.png", class: "h-auto min-[288px]:max-w-64 mt-0 mb-4 md:mb-0 md:mr-8" %>
    </div>
    
    <div class="w-full flex items-center justify-center mx-0 mb-4 md:mx-4 md:mb-0 md:justify-start">
      <%= print_home_link %>

      <% if user_signed_in? %>
        <%= print_get_started_link %>
        <%= print_edit_profile_link %>
        <%= print_sign_out_button %>
      <% else %>
        <%= print_sign_in_link %>
      <% end %>
    </div>

    <div class="grow flex justify-end md:ml-8">
      <%= print_username %>
    </div>
  </div>
</nav>
